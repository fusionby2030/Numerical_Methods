import numpy as np

values = np.array([1, 0.5, 0.2, 0.05, 0.02, 0.01])
population = np.array([1, 1, 2, 1, 1, 3])

np.sum(values*population)
np.round(np.random.normal(200, 20, size = 6))

def is_two(p):

istwo()
